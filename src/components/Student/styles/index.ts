import { Student } from './base'
import { imageContainer, simpleContainer } from './containers'
import { tagInput } from './input'

const styles = {
  Student,
  imageContainer,
  simpleContainer,
  tagInput
}

export default styles